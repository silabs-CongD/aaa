name: test

on:
  pull_request_target:

  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch TPHD to test'
        type: string
        default: 'master'    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: inputs
      run: |
        echo ${{ github.ref }}
        
    - name: Checkout
      uses: actions/checkout@v4.1.7
      with:
        ref: "${{ github.event_name == 'workflow_dispatch' && github.event.inputs.branch || github.ref }}"
        repository: silabs-CongD/aaa
